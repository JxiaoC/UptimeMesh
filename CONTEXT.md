# UptimeMesh

分布式主动监控系统:Dashboard 负责配置、任务下发与结果汇总,检测全部由各处的 Agent 执行。

## Language

**Dashboard**:
面向用户的中心服务端,负责监控配置、任务下发、结果汇总、统计展示与告警。自身不执行任何检测。
_Avoid_: Server(含义过宽)、Console

**Agent(节点)**:
部署在各地、主动向 Dashboard 建立长连接的检测执行进程。UI 中文标签为「节点」,代码/API 中统一用 Agent。
_Avoid_: Node、Probe、Worker

**Region(地域)**:
节点所在的国家/地区,用 ISO 3166-1 alpha-2 大写码(CN/HK/US…)表示,UI 以国旗 + **当前界面语言**的名称展示(默认中文,可在右上角切英文)。默认按节点来源 IP 自动解析(本地 MaxMind GeoLite2 库:由 `GEOIP_DB_PATH` 指向 `.mmdb`,进程内查询、离线可用;仪表盘镜像在构建时下载该库并打进 `/app/geoip/`,可用 `GEOIP_MMDB_URL` 换源、用 `deploy/fetch-geoip.sh` 在宿主上更新后挂载)。**内网来源 IP 不解析地域**:本机回环(127/8、::1)、私有网段(10/8、172.16/12、192.168/16、fc00::/7)与链路本地(169.254/16、fe80::/10)分别显示「本机 / 局域网 / 链路本地」(见 `geoip.ScopeOf` 与接口字段 `netScope`),此时 `region` 恒为空,不会套用该节点上一次解析出的国家。公网节点可手动指定覆盖,清空手动值即恢复自动解析。展示译名遵循一个中国原则:HK/MO/TW 在中文下为「中国香港 / 中国澳门 / 中国台湾」,英文下为 Hong Kong, China / Macao, China / Taiwan, China —— 由 `web/src/utils/region.ts` 的覆盖表保证(不能只靠 `Intl.DisplayNames`:它在中文下会给出「中国香港特别行政区」与「台湾」)。地域仅是展示与运维辅助信息,不参与调度与告警判定。
_Avoid_: 地区、位置、Location

**Monitor(监控)**:
一个被监控的目标及其检测配置(周期 10~3600 秒、探测超时、期望条件、告警阈值与连续轮数、指派到哪些 Agent、通知渠道)。五种类型:HTTP(S) 与 PING、**TCP 端口**(与目标建立 TCP 连接即算成功)、**外部上报**(没有目标,由外部系统上报结果)与**下载速度**(URL 是文件地址,节点下载测速)。UI 中文标签为「监控」。可选带一个**分组**标签用于归类展示,另可指定探测使用的 **IP 协议族**。
_Avoid_: Site、URL、检查项

**IP 协议族(IP Version)**:
Monitor 上的一个可选项:`自动`(默认,由系统按目标解析结果选择)、`IPv4`、`IPv6`。选具体族时节点**只解析、只连**该族(`tcp4`/`tcp6`、`udp4`/`udp6`、ICMP/ICMPv6),目标没有该族地址即判失败 —— 混合解析的主机名不会"悄悄"落到另一族,否则"这条监控到底测了哪条链路"说不清。它只约束**节点到目标**这一段探测,不影响节点↔Dashboard 的长连接。节点自身是否具备某族出口地址由节点在 hello/心跳里自报(见「网络能力」),监控表单据此提示"当前没有任何节点具备 IPv6"。外部上报没有节点探测,该选项不适用。
_Avoid_: IP 版本、协议栈(会读成节点侧配置)

**网络能力(Net Capability)**:
Agent 自报的本机 IPv4 / IPv6 可用性:存在非回环、非链路本地的该族地址即为具备(只看本地接口、不主动外连,内网/离线节点同样能给结论)。随 hello 与每 10s 的心跳上报,仪表盘只在取值变化时落库;节点页显示为三个标签态 —— 具备(绿)、不具备(红)、未上报(灰,老版本 Agent),三态不合并:"未上报"不等于"不支持"。它回答的是"这台机器能不能用该族去探测目标",不参与调度与告警判定。
_Avoid_: 网络状态、连通性(都太宽,和在线/离线混淆)

**外部上报(Push)**:
一种没有探测目标的监控类型:每个监控有一个免登录的上报地址 `/api/push/{token}`(令牌即凭据,与 UptimeKuma 的 push 监控同形),外部系统(路由器脚本、CI、定时任务)按周期请求它报告结果,参数 `status=up|down`、`msg`、`ping`。每次上报生成一轮单样本结果并驱动统计与告警;超过一个周期没有任何上报,调度器按周期补 0% 的静默轮,连续失败轮数用完后判 DOWN —— 因此"多久算失联 = 周期 × 连续失败轮数"。**这类监控没有可用的「可用率阈值」**:每轮只有一个样本,本次成功率非 0% 即 100%,阈值填多少判定结果都一样((0,100] 区间内完全等价),而 0 会让判定式 `成功率 < 阈值` 恒为假、该监控永不告警。故表单不展示这一项,存储与判定一律固定为 `store.PushThreshold`(100),语义即"这一轮没有成功上报就是失败轮";可调的是**连续失败轮数**。反转模式对它有正当用途(上报故障算正常),但只翻转**上报的判定**,静默轮不参与反转 —— 反转的是"上报说了什么",不是"有没有上报",否则反转监控静默再久也不告警。从未上报过的监控以创建时间为基准,同样会逐步判 DOWN。Dashboard 只接收上报、不主动探测(见 ADR-0002)。
_Avoid_: 被动监控、心跳监控(都容易被误解成"没有上报即 DOWN"之外的东西)、Webhook 监控(UptimeMesh 的 Webhook 是通知渠道)

**TCP 端口监控**:
Monitor 的一种类型:对 `主机:端口` 建立 TCP 连接即算成功,不做应用层交互;由 Agent 执行探测。对应 UptimeKuma 的 TCP Port 监控。
_Avoid_: 端口探测(口语)、Socket 监控

**下载速度监控**:
Monitor 的一种类型:URL 指向一个**文件**,各 Agent 每轮把它完整下载一遍(只统计字节数与耗时,不落盘、不保留正文),回传测得的平均速度;2xx 表示下载成功。请求侧配置与 HTTP 监控相同(方法/请求头/请求体/超时/忽略证书校验,含 Host 头选源与跟随重定向),但判定对象是速度:**告警阈值即下载速度阈值**(单位可选 `KB/s` / `MB/s`),本次平均速度低于它即「低于阈值轮」。本次成功率仍然照旧统计(它回答"文件下得下来吗"),故可用率窗口对这类监控表示**可下载率**;反转模式不适用于它(反转只作用于探测成败)。速度在接口与存储里统一用 KB/s,只有录入与展示按配置单位换算。调度上**监控内串行**:一轮里逐节点测速(同一时刻只有一个节点在下载),上一轮没定稿不开下一轮 —— 同时下载会互相压带宽、也会压垮目标文件服务器,测出来的都不是这条链路的真实速度。见 `.scratch/download-speed-monitor/spec.md`。
_Avoid_: 测速监控(口语,与带宽测试混淆)、网速监控

**下载速度阈值**:
下载速度监控专用的告警阈值口径:Monitor 的 `threshold` 字段在这类监控上不是百分比,而是速度下限,单位由 `speedUnit`(`KB/s` / `MB/s`)决定。判定值是**本轮各有效样本速度的算术平均**(下载失败或活着没回结果的样本按 0 计入,离线缺样不计入分母)。
_Avoid_: 带宽阈值、速率阈值

**反转模式(Invert Mode)**:
Monitor 上的一个可选开关(默认关闭),把**探测判定**整体取反:探测失败算正常、探测成功算故障。用于监控「本就该不可达/该出错」的目标——维护页、按预期应被拦截的接口、应保持离线的备用系统;这类目标「恢复正常」才是需要人介入的事件。反转只作用于判定层:本次成功率、可用率、告警阈值、连续低于阈值与通知模板全部沿用既有口径;缺样(节点活着没回、节点离线)说的是节点而非目标,不参与反转。配置在建轮时随阈值一起快照,故只对下一轮起效,历史轮次不重算。
_Avoid_: 反向监控(口语)、Upside Down(仅作英文对照)

**期望条件取反**:
反转模式**不是**把期望条件取反(比如「状态码不在 200~299 内算成功」)。那是另一件事,本系统不提供;需要这类语义时用反转模式整体取反。

**JSON 断言(JSON 查询)**:
HTTP 监控上的可选断言:用 JSONata 表达式从响应中取值,把取值与期望值都转成字符串后按比较方式(等于/不等于/包含/大于/大于等于/小于/小于等于,缺省等于)比较,不满足即判该次探测失败。响应体不是合法 JSON 时按原始字符串处理,故表达式 `$` 可拿到原始响应;取值必须是单个原始值(数组、对象、空值一律判失败)。语义与 UptimeKuma 的「HTTP(s) - JSON 查询」一致,导入时原样搬过来;表达式在保存监控时校验可编译,求值在 Agent 侧完成(见 `.scratch/json-assert/spec.md`)。
_Avoid_: JSONPath(那是另一种语法,本系统用的是 JSONata)

**Group(分组)**:
Monitor 上的一个自由文本标签,用于把监控归类。总览页按分组以「卡片套卡片」聚合展示;监控页提供下拉框按分组过滤(默认「全部」)。空值表示「未分组」。分组只是展示维度,不参与指派、调度或告警判定。
_Avoid_: Tag、Category、Folder

**Assignment(指派)**:
Monitor 与某 Agent 之间的执行分配关系。一个 Monitor 可指派给多个 Agent,各 Agent 独立按周期执行。指派有三种同级模式:**指定节点**(固定节点列表,仅列出的节点参与)、**所有节点**(全部已批准节点参与,建轮时动态解析,后续新增自动纳入)、**排除节点**(全部已批准节点中排除指定节点,其余含后续新增均参与)。后两种模式无需在新增节点后编辑监控。

**Probe Round(探测轮次)**:
Dashboard 按监控周期发起的一次逻辑探测:创建轮次、下发给全部被指派 Agent、收齐(或截止)后聚合。是"同一次探测"的归属单位,多 Agent 结果按轮次聚合成该轮成功率。新建的监控首见即建轮;**编辑保存或暂停后恢复也会立刻补一轮**(不等满一个周期,周期最大 3600 秒);此后**按周期整拍推进**:轮次的计划时间落在以首轮为原点的网格上(60 秒周期即 `…11:11:11 / 11:12:11 / 11:13:11`),实际的建轮与下发只会晚于它不到一个调度 tick,Dashboard 侧的延迟不会被吃进下一个周期(见 `.scratch/schedule-drift/spec.md`);**节点(重新)接入时会补发在途轮次里"还没交给它、且剩余时间够跑一次探测"的任务**(重启后首轮的兜底,见 `.scratch/restart-first-round/spec.md`);下载速度监控一轮里逐节点串行测速,故一轮耗时随节点数变长、上一轮未定稿不开下一轮。见 `.scratch/immediate-round-on-change/spec.md` 与 `.scratch/download-speed-monitor/spec.md`。
_Avoid_: 批次、任务(与下发消息混淆)

**检测结果(Check Result)**:
某 Agent 对某 Monitor 某个轮次一次执行的记录:成功/失败、响应耗时或往返延迟、错误信息、执行时间;必须携带轮次 ID。PING 每轮只发 1 个包,结果只有成功/失败两种。TCP 端口监控连接成功即成功并记录建连耗时。HTTP 监控跟随重定向(上限 10 跳)后按最终响应判定。下载速度监控记录本次下载的字节数与平均速度(KB/s)。

**This Round Success Rate(本次成功率)**:
单个轮次内的成功样本数 ÷ 有效样本数(不计入的缺样见"缺样")。对非下载速度监控而言它就是告警判定的基本单位;下载速度监控的判定值是**本次平均下载速度**,成功率在这类监控上表示"可下载率"(只用于可用率统计与节点明细展示)。
_Avoid_: 轮内成功率(旧说法,UI 与文档已统一为「本次成功率」)

**Availability(可用率)**:
一段时间窗口内全部有效结果汇总的成功占比(如 24h/7d/30d),用于列表与详情页展示,不直接驱动告警。
_Avoid_: 报错百分比(口语)

**Alert Threshold(告警阈值)**:
Monitor 配置的判定值下限:普通监控是本次成功率下限(如 80%),下载速度监控是下载速度下限(单位 `KB/s` / `MB/s`),外部上报没有可调空间、恒为 100(每轮只有一个样本,见「外部上报(Push)」)。轮次判定值低于阈值 ⇒ 该轮记为"低于阈值轮"。
_Avoid_: 破线轮(旧说法,UI 与文档已统一为「低于阈值」)

**Consecutive Breach(连续低于阈值)**:
最近连续 N 个轮次(默认 3,每个 Monitor 可配)全部为低于阈值轮 ⇒ Monitor 判定 DOWN 并触发告警。
_Avoid_: 最近N次、连续破线(旧说法,UI 与文档已统一为「低于阈值」)

**State Change(状态变动记录)**:
Monitor 告警状态的一次翻转(UP→DOWN 或 DOWN→UP)留下的记录:只在真翻转时写一条,连续低于阈值期间不重复;记录指向触发翻转的那一轮,详情页便与轮次时间线同源展示该轮的状态、本次成功率与节点明细,时间列取**变动时间**(该轮定稿、状态真正翻转的时刻,同「变动时间」词条),轮次时间线那列才是该轮的**计划时间**。总览页另有一张跨监控的「最近状态变动记录」流水(最近 20 条,只列监控名、变动方向、变动时间与该轮判定值,`GET /api/v1/state-changes`),与详情页那张表同源不同视图。UNKNOWN 轮冻结状态机、暂停/恢复与编辑阈值都不算变动;记录自本能力上线起积累,不回填历史(翻转前后的阈值与连续轮数是历史快照,重放会得出假记录)。见 `.scratch/state-change-history/spec.md`。
_Avoid_: 变更历史、事件日志(会把配置变更混进来)

**Alert Duration(报警持续时长)**:
一次报警从状态机判 DOWN 到恢复 UP 的时长,= 恢复那条状态变动记录的 `changed_at` 减去上一条报错记录的 `changed_at`(起点是判 DOWN 的那一刻,不是"第一次低于阈值那一轮")。只存在恢复那条记录上(`monitor_state_changes.duration_sec`,由调度器在落记录时算好),两个 state-changes 接口与 `monitor_flipped` 推送同名为 `durationSec`;**0 = 没有可配对的报错记录**(监控开局就是 DOWN、或配对记录已删),展示时按"无时长"处理而不是「0 秒」。前端在总览流水的「恢复」标签旁、详情页变动列的 `DOWN → UP` 旁各显示一张时长卡;存量历史不回填。见 `.scratch/alert-duration/spec.md`。
_Avoid_: 故障时长、MTTR(前者易与"低于阈值"那段混淆,后者是统计口径而不是单次记录)

**Notification Template(通知模板)**:
告警状态翻转时发往通知渠道的标题与正文,按事件类型(DOWN / UP / TEST)分别配置。正文支持 `{{变量}}` 占位符(如 `{{monitorName}}`、`{{url}}`、`{{successRate}}`、`{{speed}}`、`{{agents}}`、`{{errorCount}}`、`{{timestamp}}`),发送时替换为实际值;`{{speed}}` 仅下载速度监控有值(形如 `12.34 MB/s`),这类监控的**默认**模板讲「平均下载速度」。`{{agents}}` 是触发翻转那一轮的**节点明细**多行文本(每个被指派节点一行:正常/失败/未回传/离线,失败带状态码或原因),由结果表里的按时结果 + 调度器给出的缺样名单合成 —— 只有成功率看不出"是谁挂了",值班的人第一步要问的正是这个。`{{errorCount}}` 是渲染这一刻仍处于报警状态的**启用中**监控数(DOWN 事件含本条监控,UP 事件是恢复后剩下的;暂停中的监控只保留告警状态、不再告警,故不计入),DOWN/UP 的内置默认模板把它顶在标题最前方(`【{{errorCount}}报警】monitorName`),一眼能看出是单点故障还是整片挂了;统计读库失败时该变量渲染为空串。标题与正文均清空表示该事件回落内置默认模板(DOWN/UP 的默认正文含「节点状态」一段)。模板只是文案,不改变投递目标与重试策略。
_Avoid_: 消息格式、推送文案

**Webhook Body Template(请求体模板)**:
单个通知渠道可选配的请求体结构,用于适配不同机器人要求的 JSON 形态(钉钉/企微/Slack 等)。可用变量为事件变量加上已渲染的 `{{title}}`/`{{content}}`,变量值按 JSON 规则转义;留空则发送默认通用 JSON。它决定"发什么结构",与通知模板决定的"文案"相互独立。
_Avoid_: 消息体、payload 模板

**缺样(Missing Sample)**:
轮次收集截止时某 Agent 未交回结果。Dashboard 对每个缺样 Agent 探活一次,再结合"本轮任务有没有真的交给过它"分三种处置:探活无响应 ⇒ **离线缺样**、不计入分母;探活有响应**而且**任务交给过它 ⇒ 该样本**记为失败**;探活有响应**但任务从未交给它**(建轮时它不在线、节点重连后的补发又没赶上)⇒ **未派发任务**、不计入分母。第三种单独留痕,因为成因在派发侧而不是"节点测不出目标"——把在线节点标成「离线」和线上原来的「超时(Agent在线)」一样是在说假话。重启后的首轮靠"重连补发"把这类样本救回成真实结果,详见 `.scratch/restart-first-round/spec.md`。
_Avoid_: 漏报、无数据

**Heartbeat(心跳)**:
Agent 周期性向 Dashboard 上报的存活信号;丢失心跳将 Agent 标记为离线,但不等同于其监控目标失败。

**分发版本(Dist Version)**:
仪表盘安装包目录里可分发给节点的 Agent 版本(`VERSION` 标记,与二进制内 `-ldflags` 打标的版本必须一致)。节点上报的版本与分发版本不同即视为「版本落后」;它只描述版本差异,不代表新旧顺序——把整个节点群对齐到仪表盘当前分发的版本才是目标。
_Avoid_: 最新版本(隐含"更高"的比较语义)

**一键升级(Agent 自升级)**:
节点页上对某个已批准节点的操作:节点自行从仪表盘下载分发版本、校验内容、替换自身可执行文件并原地重启,不需要登录目标机。Dashboard 只下发目标版本与 sha256(二进制仍走公开的安装包分发端点),因此不必在节点侧开额外端口;仅 Linux 节点支持,且节点需对自身可执行文件所在目录有写权限。为了不把指令发给根本不认识它的旧版本,节点在 `hello` 里声明 `self_upgrade` 能力,未声明的节点在页面上标「需重装」而非给出按钮。链路细节见 `docs/protocol.md`。
_Avoid_: 远程升级、OTA(都暗示另开一条推送二进制或执行命令的通道)

**UptimeKuma 导入**:
设置页的一次性迁移能力,两种取数方式:①API 密钥,经 UptimeKuma 的 `/metrics`(Prometheus 文本,密钥即 Basic Auth 凭据)读取,只有名称/类型/目标;②账号密码,登录其 Socket.IO 管理接口(1.x/2.x 正式版走 `login` 事件,去掉该事件的新版走 better-auth 会话 Cookie)取回 `monitorList` 全量配置,可带请求方法/请求头/请求体、关键词、期望状态码、检测间隔、JSON 查询断言、反转模式与所属分组。取回的监控映射为 UptimeMesh 的五种类型中 Kuma 有对应物的四种(HTTP / PING / TCP 端口 / 外部上报)后批量创建,已存在的同目标(含本次导入内重复)与不支持的类型自动跳过。Kuma 没有「下载速度」类型,故该类型不会被导入。UptimeKuma 的 `group` 是容器、不是探测目标:它本身不导入,名称成为子监控的「分组」(嵌套拼成「父 / 子」);表单里的「导入到分组」只兜底未分组的监控。**外部上报**监控导入后生成 UptimeMesh 自己的上报地址,需要把上报方从 Kuma 的地址改指向 UptimeMesh。字段只在 Kuma 对应类型上生效:关键词取自 `keyword` 类型、JSON 断言取自 `json-query` 类型(宽表里其它类型的残留值不搬,否则会凭空多出永远失败的校验);**在 Kuma 里暂停的监控导入后同样保持暂停**,不受"立即启用"影响,也可勾选把已存在同目标监控的启用/暂停对齐到 Kuma。只导入配置,不迁移历史检测数据;这是一次管理员主动触发的配置读取,不构成检测(见 ADR-0002)。
_Avoid_: 同步、对接(均暗示持续双向)

**配置文件导入导出(Config Import/Export)**:
设置页的一次性能力,把一个 UptimeMesh 实例的**可迁移配置**导出成一份 JSON 文件,或把这样一份文件导入另一个实例(换机迁移、批量备份、按文件建一批监控)。文件内容 = 监控(五种类型与全部探测细节)+ 通知渠道 + 通知模板 + 面板设置(原始结果保留期、最近状态格数);**不含**接入密钥与 JWT 密钥、管理员账号密码、节点记录与凭据,也不含轮次/结果/统计/状态变动等历史数据 —— 配置与数据是两件事。跨实例的引用按**稳定的人类可读键**对齐:渠道用名字(渠道 ID 是实例内主键),节点指派不进文件(节点 ID 属于各实例),指派一律由导入表单统一指定(默认「所有节点」),文件里记录的指派方式只作对照。监控的判重、覆盖与逐条容错沿用 UptimeKuma 导入的口径(类型 + 目标判重;默认只新建,可选覆盖重写且保留监控 ID 与历史轮次)。**外部上报**监控的上报令牌随文件走(同产品迁移,已部署的上报脚本不用改指向),与库内其它监控冲突或文件里缺失时重新生成。版本规则是这条能力的核心:文件带 `configVersion` 单一闸门,**低版本文件直接导入**(缺的字段按默认值补齐),**更高版本的文件一律拒绝**并提示先升级仪表盘 —— 因为高版本文件可能含本版本不认识的配置,静默导入等于悄悄丢配置。相应地有一条维护规则:导出结构任何变化(新增/删除字段、改变字段语义)都必须让 `configVersion` +1 并补一条迁移。见 `.scratch/config-import-export/spec.md`。
_Avoid_: 备份、快照(都暗示含数据,而这里只有配置)、同步(暗示持续双向)
