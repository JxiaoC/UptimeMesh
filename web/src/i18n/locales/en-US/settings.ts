import zhSettings from '../zh-CN/settings'

const settings: typeof zhSettings = {
  settings: {
    title: 'System settings',
    tabs: {
      general: 'Settings',
      security: 'Security',
      templates: 'Notification templates',
      database: 'Database usage',
      import: 'Import UptimeKuma',
      config: 'Config import/export',
    },
    security: {
      username: 'Login username',
      usernamePlaceholder: 'The username used to sign in to the dashboard',
      usernameHelp: '2–32 characters, no spaces.',
      currentPassword: 'Current password',
      currentPlaceholder: 'Enter the current password',
      currentHelp: 'Changing the username or password always requires the current password; if it is lost, reset it on the server (remove the users row and run the setup flow again).',
      newPassword: 'New password',
      newPlaceholder: 'Leave empty to change only the username',
      newHelp: '6–72 characters; leave empty to keep the current password.',
      confirmPassword: 'Confirm new password',
      confirmPlaceholder: 'Enter the new password again',
      save: 'Save account settings',
      saved: 'Account settings saved',
      needUsername: 'Enter a login username',
      needCurrent: 'Enter the current password',
      mismatch: 'The two new passwords do not match',
      note: 'Changing the password does not sign other browsers out immediately (the login state is a signed token valid for 7 days; the server keeps no sessions). To force every session offline, replace the JWT secret in your deployment and restart the dashboard.',
    },
    retention: {
      label: 'Keep raw results (days)',
      help: 'Defaults to 31 days, adjustable from 1 to 365. Hourly aggregates (the data source for the 24h/7d/30d availability rates) are kept for a fixed {days}-day period regardless of this setting, and anything older is cleaned up too; the per-agent latency chart on the detail page reads raw results and only covers samples inside the retention window.',
      saved: 'Saved — the background cleanup job will use the new retention period',
    },
    stripRounds: {
      label: 'Recent status cells',
      help: 'How many round cells the "Recent status" column on the monitors page shows: {min}–{max}, {def} by default. More cells reveal a longer trend, but the column gets wider and the page renders more; the new count applies when you return to the monitors page.',
      saved: 'Saved — the monitors page will use the new cell count',
    },
    grace: {
      label: 'Round collection grace period',
      value: '{seconds} seconds (a round is finalized at probe timeout + grace period)',
    },
    key: {
      label: 'Enrollment key',
      hashOnly: 'This database only stores the key hash (an early version), so the plaintext cannot be recovered — please rotate it once.',
      rotate: 'Rotate enrollment key',
      help: 'New agents use this key for their first connection; the dedicated credentials of approved agents are unaffected by rotation.',
      copied: 'Enrollment key copied',
      rotateConfirm: 'After rotation the old enrollment key stops working immediately: new agents must use the new key for their first connection; dedicated credentials of approved agents are unaffected.',
      rotateConfirmTitle: 'Confirm enrollment key rotation',
      rotated: 'New enrollment key (copy and save it now):<br/><code style="font-size:14px;user-select:all">{key}</code>',
      rotatedTitle: 'Rotation complete',
      savedIt: 'I saved it',
    },
    misc: {
      help: 'Other configuration (SQLite file path, listen port, JWT) lives in environment variables and config files — deploy/.env and the dashboard config; restart the container to apply changes.',
    },
    templates: {
      intro: 'Configure the title and body sent to notification channels for each event. Placeholders are supported and replaced with actual values when sending:',
      clickToInsert: 'Click a variable to insert it at the end of the input',
      titleLabel: 'Title',
      bodyLabel: 'Body',
      titlePlaceholder: 'Notification title',
      bodyPlaceholder: 'Notification body, may be multi-line',
      preview: 'Preview (sample values)',
      noTitle: '(no title)',
      save: 'Save templates',
      reset: 'Restore this event default',
      resetTip: 'Clearing the title and body and saving restores the default template',
      saved: 'Notification templates saved',
      samples: {
        monitorName: 'Marketing site',
        agents: '- APAC-1: failed (HTTP 502)\n- US-2: OK (18 ms)',
        // {{duration}} 的预览示例:仅恢复(UP)事件有值,发送时按实际秒数折算。
        duration: '5 min 30 s',
      },
    },
    events: {
      DOWN: 'Alert fired (DOWN)',
      UP: 'Recovery notice (UP)',
      TEST: 'Test message (TEST)',
    },
    db: {
      // On demand: entering the settings page does not measure automatically (the backend may have to scan the whole file); usage is fetched only when "View usage" is clicked.
      notLoaded: 'Database usage has not been measured yet. Click "View usage" to start; measuring scans the entire database file and can take tens of seconds when the file is large.',
      view: 'View usage',
      // Measuring always runs in the background (also on the first click): the placeholder shows a computing state while polling, without blocking any request.
      computing: 'Measuring database usage (running in the background)…',
      summary: '{tables} tables and {rows} rows in total; database file {size} (single SQLite file, table usage counted per page).',
      empty: 'No tables yet',
      columnTable: 'Table',
      columnRows: 'Rows',
      columnSize: 'Table pages',
      refresh: 'Refresh usage',
      compact: 'Compact database',
      compactTip: 'Compacting rewrites the database file from the current data: it reclaims the free pages left by deleted rows and truncates the WAL, deleting no data and changing neither table structures nor query results. While it runs, the database takes exclusive write access, so result and round writes queue up — the larger the file, the longer it takes.',
      freeHint: 'The database file holds {size} of free pages (holes left by deleted or expired results) that "Compact database" can reclaim.',
      compactConfirm: 'The database will be compacted, reclaiming roughly {free} of free space. While it runs, result and round writes queue up (the larger the file, the longer it takes — possibly tens of seconds) and other actions become slower. No data is deleted. Continue?',
      compactConfirmTitle: 'Compact database',
      compactDone: 'Compaction finished: {before} → {after}, reclaimed {saved}',
      compactIdle: 'Compaction finished: the database is already compact, nothing to reclaim (currently {size})',
      compactDoneTitle: 'Compaction result',
      dailyGrowth: 'At the current check frequency of {monitors} enabled monitors, about {rounds} rounds and {results} results are added per day (roughly {size}).',
    },
    import: {
      intro: 'Read the monitor list from UptimeKuma and create them as UptimeMesh monitors in bulk. Two ways to read the data:',
      itemApiKey: '{apiKey} (recommended, simplest): reads the {metrics} endpoint, which only exposes name, type, target and current status; keywords, request method, expected status codes and check intervals are unavailable. Only monitors that have produced check data are counted (paused ones are not).',
      itemPassword: '{password} (full configuration): signs in to the UptimeKuma management interface with the account credentials (the same path as the Kuma web UI), importing keywords, request method/headers/body, expected status codes, check intervals, JSON query assertions, inverted mode and group in full; UptimeKuma "group" containers become the group label of their child monitors (nested groups are joined as "parent / child", the container itself is not imported). Credentials are used for this import only and are never stored; {versions} (the Socket.IO login event is preferred; the login session is only used for newer builds that dropped it). When the account has two-factor authentication enabled, enter the one-time code. The reverse proxy must at least allow {socketIo} (only newer builds that use the login session need {apiAuth}).',
      itemTypes: '{types}: HTTP(S), PING, TCP port and push; other types (databases, containers, DNS, gRPC, …) are still skipped with a reason. Note that {push} monitors get an UptimeMesh push URL after import, so the reporting side (router scripts and the like) must be repointed from Kuma to UptimeMesh — the URL can be copied on the monitor detail page.',
      versionsSupported: 'both the 1.x and 2.x stable releases are supported',
      typesSupported: 'Importable types',
      baseUrl: 'UptimeKuma URL',
      authMode: 'Authentication',
      authApikey: 'API key',
      authPassword: 'Account credentials (full configuration)',
      apiKey: 'API key',
      apiKeyPlaceholder: 'uk1_… (UptimeKuma Settings → API keys)',
      apikeyTip: 'Some features cannot be imported through the API: keywords, request method/headers/body, expected status codes, check intervals, JSON query assertions and the original group need the "account credentials" mode; also, only monitors that have produced check data appear in the metrics',
      username: 'Username',
      usernamePlaceholder: 'UptimeKuma login username',
      password: 'Password',
      passwordPlaceholder: 'Used for this import only, never stored',
      twoFA: 'One-time code',
      twoFAPlaceholder: 'Optional, 6 digits',
      twoFATip: 'Required when the account has two-factor authentication enabled',
      tls: 'Self-signed certificate',
      insecureTls: 'Ignore TLS certificate validation when connecting and fetching',
      group: 'Import into group',
      groupPlaceholder: 'Leave empty for ungrouped',
      groupTip: 'Used for monitors without a group; monitors already grouped in UptimeKuma keep their original group',
      periodTimeout: 'Period / timeout (s)',
      periodTipPassword: 'The account credentials mode prefers the actual UptimeKuma values; these are only a fallback',
      periodTipApiKey: 'The UptimeKuma metrics carry no interval; this value is applied to all',
      thresholdConsecutive: 'Threshold / consecutive rounds',
      roundsUnit: 'rounds',
      assignAllTip: 'Every approved agent takes part; new agents need no edit',
      afterImport: 'After import',
      enableNow: 'Enable monitors immediately',
      enableNowTip: 'Monitors paused in UptimeKuma are unaffected and stay paused after import',
      existing: 'Existing monitors',
      syncPaused: 'Align enabled/paused with the current UptimeKuma state',
      syncPausedTip: 'By default only new monitors are created and existing ones are left alone; when checked, monitors with the same target get the paused state aligned',
      overwrite: 'Overwrite the configuration of existing monitors with the same target (keywords / JSON assertions / period / status codes …)',
      overwriteTip: 'Use this to repair wrong configuration left by an earlier import (for example a plain HTTP monitor that got keywords or JSON assertions); monitor IDs and round history are kept, and the local enabled state only follows Kuma when Kuma has it paused',
      preview: 'Test connection and preview',
      doImport: 'Import now',
      importableCount: '{count} importable',
      summary: 'Connected to {endpoint}: {total} monitors — {importable} importable · {duplicate} duplicate targets · {unsupported} unsupported',
      summaryGroups: '; {count} group containers were turned into group labels of their child monitors',
      summaryPaused: '; {count} of them are paused in UptimeKuma and stay paused after import',
      previewEmpty: 'No monitors were read',
      columnKumaType: 'UptimeKuma type',
      columnMappedTo: 'Mapped to',
      columnMethod: 'Method',
      columnMappedGroup: 'Group',
      columnKumaStatus: 'Status',
      columnOutcome: 'Outcome',
      columnReason: 'Notes',
      byParam: 'From parameters',
      byImportParam: 'From import parameters',
      pushNoTarget: 'Push (no target)',
      tagPaused: 'paused',
      outcomeUnsupported: 'Unsupported',
      outcomeDuplicate: 'Duplicate target',
      outcomeImportable: 'Importable',
      resultTitle: 'Import result',
      resultSummary: '{created} created · {skipped} skipped · {failed} failed',
      columnResult: 'Result',
      created: 'Created',
      notCreated: 'Not created',
      warnBaseUrl: 'Please fill in the UptimeKuma URL first',
      warnApiKey: 'Fill in the UptimeKuma API key (or switch to account credentials)',
      warnCredentials: 'The account credentials mode needs both the UptimeKuma username and password',
      warnAssign: 'With "Selected agents" you must pick at least one agent',
      confirmImport: '{importable} monitors will be created; {duplicate} duplicate targets (already existing or duplicated within this import) and {unsupported} unsupported types are skipped automatically.',
      confirmImportTitle: 'Confirm UptimeKuma import',
      importDone: 'Import finished: {created} created, {skipped} skipped',
      importDoneUpdated: ', {count} overwritten',
      importDoneSynced: ', {count} paused states aligned',
    },
    exportImport: {
      intro: 'Export this instance\u2019s migratable configuration into one JSON file, or import such a file: monitors (all five types with every probe detail), notification channels, notification templates and panel settings (result retention, status strip length).',
      itemScope: 'Meant for moving to another machine, bulk backups and "create a batch of monitors from a file"; the file never contains the enrollment key, JWT secret, login credentials or agent credentials, and no historical check data.',
      itemAssign: 'Agent assignment does not travel with the file (agent IDs belong to each instance): the assignment below is applied to every imported monitor, and the assignment recorded in the file is informational only.',
      itemVersion: 'The file carries a config version: older files import directly (missing fields fall back to defaults), while a newer file is rejected — upgrade the dashboard first, so no unrecognized configuration is silently dropped.',
      exportTitle: 'Export',
      exportButton: 'Export config file',
      exportHint: 'Downloads a JSON file you can read and diff in any editor; keep it safe (it contains the push tokens of external-report monitors).',
      exportDone: 'Exported: {monitors} monitors, {channels} channels, {templates} custom templates',
      importTitle: 'Import',
      pickFile: 'Choose a config file',
      fileChosen: 'Selected: {name}',
      fileMeta: 'Config version v{version} · exported at {exportedAt} · this dashboard supports v{supported}',
      fileMetaNoTime: 'Config version v{version} · this dashboard supports v{supported}',
      badJson: 'This file is not valid JSON — please pick a file downloaded from "Export config file"',
      preview: 'Preview import',
      doImport: 'Import now',
      changeFile: 'Choose another file',
      assignTip: 'The assignment in the file is informational only; the choice below is what gets applied',
      overwrite: 'Overwrite existing monitors with the same target',
      overwriteTip: 'By default only new monitors are created and existing targets are skipped; when checked, they are rewritten from the file (monitor ID and history are kept, while period/threshold etc. follow the file)',
      importSettings: 'Also import panel settings (retention / status strip length / notification templates)',
      importSettingsTip: 'Notification templates are replaced as a whole: events missing from the file fall back to the built-in defaults — check the preview notes',
      previewSummary: '{monitors} monitors — {create} to create · {update} to overwrite · {duplicate} duplicate targets · {invalid} invalid',
      previewChannels: '; channels: {create} new · {reuse} reused · {invalid} invalid',
      previewSettings: '; panel settings: {keys}',
      previewNoSettings: '; the file has no panel settings, local settings stay untouched',
      previewEmpty: 'The file contains no monitors',
      settingsChanges: 'Panel setting changes',
      settingsCurrent: '{label}: current {current} → file {next}',
      settingsTplCount: '{count} custom notification templates',
      settingsTemplates: 'Notification templates',
      listSeparator: ', ',
      settingsTplOverwrite: '{event}: written from the file',
      settingsTplReset: '{event}: absent from the file, reset to the built-in default',
      columnStatus: 'Outcome',
      statusCreate: 'Create',
      statusUpdate: 'Overwrite',
      statusDuplicate: 'Duplicate target',
      statusInvalid: 'Invalid',
      channelTitle: 'Notification channels',
      channelReuse: 'Reuse by name',
      channelCreate: 'Create',
      channelInvalid: 'Invalid',
      warningsTitle: 'Notes',
      confirmImport: '{create} monitors will be created, {update} overwritten, {duplicate} skipped and {invalid} invalid entries ignored. {settings}Continue?',
      confirmImportTitle: 'Confirm config import',
      confirmSettings: 'Panel settings and notification templates will be replaced by the file; ',
      confirmNoSettings: 'local panel settings stay untouched; ',
      importDone: 'Import finished: {created} created, {updated} overwritten, {skipped} skipped',
      importDoneInvalid: ', {count} invalid',
      importDoneChannels: '; channels: {created} new, {reused} reused',
      importDoneSettings: '; panel settings updated: {keys}',
      resultTitle: 'Import result',
      resultSummary: '{created} created · {updated} overwritten · {skipped} skipped · {invalid} invalid · {failed} failed',
      resultOk: 'Done',
      resultNotOk: 'Skipped',
      needFile: 'Choose a config file first',
    },
  },
}

export default settings
