# docker compose 启动前复制为 .env 并按需修改。
# 数据落在命名卷 uptimemesh-data,容器内路径由 SQLITE_PATH 指定(默认 /data/uptimemesh.db)。

# 本地示例节点(docker compose --profile agent up -d)所需:
# AGENT_ENROLLMENT_KEY=<设置页轮换/初始密钥>
# AGENT_NAME=docker-agent

# 节点地域库:仪表盘镜像已内置 GeoLite2-Country.mmdb,通常无需设置。
# 想在容器外维护自己的库时,先跑 sh deploy/fetch-geoip.sh(或自行下载官方 .mmdb),
# 再取消 docker-compose.yml 里那行挂载,并按需覆盖路径:
# GEOIP_DB_PATH=/app/geoip/GeoLite2-Country.mmdb
